<div class="footer">
        <img src="svg/logo-dark.svg" class="logo-stamp">
        <div>
            <p>г.Алматы</p>
            <p>+7 (700) 788-81-07</p>
            <div class="social">
                <a href="https://wa.me/77007888107" target="_blank"><img src="svg/whatsapp.svg" alt=""></a>
                <a href="https://t.me/vladvlos"><img src="svg/telegram.svg" target="_blank" alt=""></a>
            </div>
            <p class="copy">© 2018 Mangi. Все права защищены.</p>
        </div>
        <div>
            <div class="menu foot">
                <a href="/">Главная</a>
                <a href="/projects">Работы</a>
                <a href="/contacts">Контакты</a>
            </div>
            <form class="newsletters" action="/subscribe" method="POST">
                <input class="btn" placeholder="Введите свой e-mail" type="email" name="email">
                <input class="btn" type="submit" value="Подписаться">
            </form>
        </div>
    </div>
    <script>
        var last_known_scroll_position = 0;
        var ticking = false;
        var isopen = false;

        function doSomething(scroll_pos) {
            if (scroll_pos > 150) {
                var x = document.getElementById("menu");
                x.classList.add("blacked");
            } else {
                var x = document.getElementById("menu")
                x.classList.remove("blacked");
            }
        }
        
        function goto(url) {
            var win = window.open(url, '_blank');
            win.focus();
        }

        document.addEventListener("DOMContentLoaded", function() {
            var menu = document.getElementById("nav-icon4");
            var overlay = document.getElementById("overlay");
            var loader = document.getElementById("loader");
            menu.onclick = function(e){
                if(!isopen) {
                    menu.classList.add("open");
                    overlay.classList.add("open");
                    isopen = true;
                } else {
                    menu.classList.remove("open");
                    overlay.classList.remove("open");
                    isopen = false;
                }
            };
            setTimeout(() => {
               loader.classList.add("loaded");
               setTimeout(() => { loader.style.display = "none"; }, 500)
            }, 1000);
        });

        window.addEventListener('scroll', function (e) {
            last_known_scroll_position = window.scrollY;
            if (!ticking) {
                window.requestAnimationFrame(function () {
                    doSomething(last_known_scroll_position);
                    ticking = false;
                });
                ticking = true;
            }
        }); 
    </script>
</body>

</html>